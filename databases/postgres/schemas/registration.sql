CREATE TABLE registration(
   id INT PRIMARY KEY     NOT NULL,
   name           TEXT    NOT NULL,
   age            INT     NOT NULL,
   address        CHAR(50),
   username       TEXT    NOT NULL,
   password       TEXT    NOT NULL
);
